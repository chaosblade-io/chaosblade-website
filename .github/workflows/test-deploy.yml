name: Test deployment

on:
  pull_request

jobs:
  test-deploy:
    name: Test deployment
    runs-on: ubuntu-latest
    steps:
      - name: Test build website
        run: echo ${{ secrets.GITHUB_TOKEN }}
